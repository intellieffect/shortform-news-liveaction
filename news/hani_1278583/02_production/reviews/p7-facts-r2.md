# fact-check 재확인 원문 (2026-09-22, 2라운드)

대상: out/pilots/hani_1278583/qa/production-e81e4dce-19d7-4d9a-99a3-ac44ca217881.mp4 (77.53s / 2326프레임). 원문 그대로.

확인한 프레임: 13s(390) · 19.2s(576) · 20.4s(612) · 22.5s(675) · 25s(750) · 28s(840) · 56s(1680) — 7장.

## ① blocking 상태 — fixed

- 13s(390): 인트로 반달 실사에 `NASA 위상 시각화 (SVS)` 라벨 이미 표기. s04 구간 시작 전부터 표기 존재.
- 19.2s(576), 20.4s(612): 경계선 확대(locator 박스 포함) 화면에도 `NASA 위상 시각화 (SVS)` 라벨 동반 — edge_zoom(562~638) 구간과 일치. 실사 구간에서 무표기 노출 없음.
- 22.5s(675): 생성 충돌구 클로즈업에 `충돌구 지형 재구성 · AI 생성` 라벨 완전히 보임 — model_prov 완전 노출 시점(648=21.6s) 이후라 라벨 없이 노출되는 프레임 없음.
- 25s(750): 동일 생성 크레이터, 라벨 유지 확인.
- 28s(840): 실제 아르테미스 2호 사진(`NASA · 아르테미스 2호 촬영`) — AI 라벨과 혼용되지 않고 정확히 구분됨.

→ 표본 구간(19.2~28s) 전체에서 라벨 없이 노출되는 실사/생성 프레임을 확인하지 못했다. 전환 지점(628~648프레임, 0.3초 구간) 자체는 직접 샘플링하지 않았으나, 그 앞뒤가 각각 실사/생성 라벨로 깨끗이 커버되어 0.3초 미만의 공백이 관측상 문제로 드러나지 않는다. **blocking → fixed로 판정.**

## ② 새 표기 사실관계

`NASA 위상 시각화 (SVS)` — RIGHTS.md S-01~S-03 크레딧과 일치. "실제 관측 사진"이라 과장하지 않고 "시각화"로 정확히 표기, "촬영"(S-06/S-07)과 표현이 구분되어 혼동 소지 없음. **적합**. intro·terminator·maria 세 개념 모두에서 라벨 확인.

## ③ verdict

**verdict: pass** (요청 범위 — blocking b-terminator-ai-label-lag 및 새 출처 표기 재확인에 한함)

incomplete(이번 요청 범위 밖): 1라운드의 F1~F13·D-01~03 나머지 구간 재검수는 수행하지 않음 — 앞 회신 원문 그대로 유효. 전환 프레임(628~648) 직접 샘플링도 하지 않음.

## 최종 파일 확인 (같은 검수자)

두 프레임 모두 직전 파일과 동일 — 19.2s에 `NASA 위상 시각화 (SVS)`, 22.5s에 `충돌구 지형 재구성 · AI 생성`이 같은 위치·같은 디자인으로 그대로 확인됨. sha256 접두 `0e5d1e09f84eafde9757dfa5` 일치.

**같음 — 방금의 fixed/pass 판정은 이 파일(`production-c2579af1-72f7-476d-a7d8-98cc3dfb141f.mp4`)에도 그대로 적용된다.**
