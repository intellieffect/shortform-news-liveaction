# 표현 레퍼런스 연결 검증 — 2026-09-22

## 수록 범위

공통 세트 2026-09-22.1: scale@v1, observation@v1(기존 승인 영상 v2), scattering@v1(최종 승인 모션그래픽). 기존 MP4를 무변경 복사했으며 case.json에 원래 파일명·SHA256·실제 입력 목록을 기록했다. 로컬 실험·계정/세션 기록은 수록하지 않았다. 원본 영상의 사용자 확정과 실제 검수 범위는 사례별 validation.md를 따른다.

## 구현 검증

- 레퍼런스 연결 테스트: 주제/사례 지정 없는 신규 시작에 3개 제공, 세트·안내 보존, 변경/누락 검출, 구형 편 비소급, 파일 경계/외부 링크, 미선정·재현 입력 누락·LFS 불일치, 등록된 미디어/문서만 HTTP 제공 및 Range 응답, experience/intent 분리.
- 기존 영상 목록 14개 테스트 및 시작·재개 19개 테스트 통과. TypeScript/runtime lint 통과.
- check:all: 기존 활성 N44 포함 오류0. registry의 기존 경고13은 남아 있다. 과거 보관 편 전체 검사는 포함하지 않았다.
- 공유 검사: staged 파일 선정·의존성·미디어 LFS 무결성 확인.
- Aside 브라우저: 세 영상 카드 표시, 산란 4.6초 탐색 재생, 프롬프트 연결 후 기사 URL 자동 입력과 공통 요청 유지, 모바일 390px iframe에서 카드/탭 배치 확인. 가로 넘침 수정. 기기 실물/전체 접근성 검증은 아니다.

## 별도 환경 재현

staged 파일만 독립 임시 폴더에 export하고 npm ci로 새 설치. 다른 worktree의 미디어/실험 캐시 없이 references:prepare로 세 사례 입력과 산란 알파 프레임을 복원했다. 세 Remotion 엔트리에서 스틸 재렌더 성공. 이 검증은 전체 MP4 재렌더/연속 시청·청취를 의미하지 않는다.

재현 PNG SHA256:
- scale f425: 66339e31950c89b96ef2fc1a4bb1d96e63ca14b4c29d1d77de345f13229a3693
- observation f150: db595849e7cac3ccb1c86175d3b494226048f4ff1427c0c9a5c3a699741bcdbc
- scattering f145: bf0d2bc3abc9b94ff311010d16c1af59eeab128fe1a06c33a6c538f8cb61b346

산란 재현 PNG는 기존 승인 영상 제작 때의 동일 프레임 PNG와 바이트 해시가 일치한다. 새 기사 한 편에서 창작 품질이 개선되는지의 실증은 이후 실제 제작에서 평가한다.

## 검토 범위

Opus가 UI 세 파일을 구현하고 구문 검사를 완료했다. 후속 독립 코드 검토는 API529 Overloaded로 실패하여 통과로 계산하지 않았다. 주 에이전트가 코드·브라우저·회귀 및 재현 결과를 확인했다. 본 작업 시작 시 주 worktree에 존재하던 시각 표현 안내 변경을 관련 기능의 기반으로 포함했다. 원래 작업 폴더의 파일은 통합 전 별도 상태로 보존했다.

설치 환경 확인: 사용자 전역 Claude 플러그인1.8.7은 다른 로컬 엔진의 marketplace를 가리켜 자동 교체하지 않았다. 호환 설치 브리지가 현재 제작 엔진을 호출해도 context.instructions는 이 저장소의 최신 지침 정본을 제공하도록 연결하고 기존 브리지 테스트로 확인했다. 설치 캐시/현재 세션 자체를 업그레이드했다고 주장하지 않는다.
